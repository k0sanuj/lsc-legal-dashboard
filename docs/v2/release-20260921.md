# Legal OS v2 release verification, 21 September 2026

## Implementation evidence

The integrated release gate passed: environment/route checks, agent hygiene,
Prisma validation, TypeScript, generation pause and provider boundaries, Slack
identity and service adapters, worker execution/cancellation, upload atomicity,
OpenSign concurrency/outbox rollback, protected byte responses, money/FX/naming/
archive boundaries, review calendar/dependency checks, dispute Finance protocol,
MNDA PDF rendering, lint and Next.js production build. Lint retained 50 warnings
and no errors. The isolated environment intentionally lacks external credentials.
The added generated-artifact verifier also passed after final source edits.

Independent reviewers executed adversarial reproductions before shipping. Fixed
findings included signed source replacement, filename proposal approval races,
large Decimal rounding, upload/review partial commits, HTML served inline,
same-millisecond storage key collision, stale signing callbacks, duplicate sends,
Finance outbox persistence, ignored CLI cancellation and missing template artifacts.

Database integration used only `legal_os_v2_verify_20260921`, with synthetic
fixtures. Actual test browser use created one clearly synthetic entity there.
No synthetic financial or legal records were added to production for these tests.

## Production prerequisites and recovery

Authorized target: GCP `fsp-legal-esign`, region `asia-southeast1`, Cloud Run
`lsc-legal-dashboard`. Default local gcloud project is unrelated.

Before the additive migration, a custom-format PostgreSQL backup was uploaded to
`gs://fsp-legal-esign-recovery/pre-v2/legal-os-pre-v2-20260921T124426Z.dump`.
Size 1,128,138 bytes; `pg_restore --list` succeeded. The recovery bucket is private,
with public-access prevention. Previous revision `lsc-legal-dashboard-00030-4ns`
is retained for code rollback. This snapshot does not cover OpenSign's MongoDB.

The production database had no Prisma migration history. The new SQL was diffed
against its actual schema, retaining existing defaults and data. The additive
migration is applied in one PostgreSQL transaction with a SHA-256 receipt.

## External acceptance remains separate

The dedicated `legal-generation-worker` VM is Debian 12 in asia-southeast1-b,
using Codex CLI 0.155.1, a private OS account, no GCP service-account credentials,
and IAP-only SSH ingress. Official device authentication is awaiting the user.
Generation is disabled until the actual login and inference receipts pass.

Google Admin requires Anuj's identity verification before mailbox inspection and
delegation. Drive destination folders/service credentials, Arvind's source table,
Copilot product/tenant, and the Finance receiver contract remain unsupplied.
The saved Finance project path does not exist locally. A mocked receiver test is
not proof of live Finance delivery. Slack has commands for 17 of 18 inventoried
workflows; real completion rates require workspace acceptance and measurement.

Live migration, revision, queue and HTTP receipts will be appended after execution.
